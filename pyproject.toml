[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [
    "site",
    "src/frontend/dist",
]

[tool.ruff.lint]
select = ["F", "I"]

[tool.ruff.lint.isort]
known-first-party = ["src"]
